# PostgreSQL User Name Maps (none needed -- local trust). Baked so the cluster's
# ident_file exists after a container recreate. AlaskaRouter-msgi.
# MAPNAME       SYSTEM-USERNAME         PG-USERNAME
